<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 offset-sm-4">
      <div class="form-login">
        <h2>Se connecter</h2>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email, required: false, autofocus: true, placeholder: "brain@gmail.com" %>
          <%= f.input :password, required: false, placeholder: "*******" %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Log in", class:'btn btn-primary' %>
        </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div> -->

<form>
    <p class="h4 text-center mb-4">Sign in</p>

    <!-- Material input email -->
    <div class="md-form">
        <i class="fa fa-envelope prefix grey-text"></i>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= f.input :email, required: false, autofocus: true, class: "form-control", id: "materialFormLoginEmailEx", label: "Your email"  %>
        <!-- <input type="email" id="materialFormLoginEmailEx" class="form-control">
        <label for="materialFormLoginEmailEx">Your email</label> -->
    </div>

    <!-- Material input password -->
    <div class="md-form">
        <i class="fa fa-lock prefix grey-text"></i>
        <%= f.input :password, required: false, class: "form-control", id: "materialFormLoginPasswordEx"%>
        <!-- <input type="password" id="materialFormLoginPasswordEx" class="form-control">
        <label for="materialFormLoginPasswordEx">Your password</label> -->
    </div>
    <div>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="text-center mt-4">
        <!-- <button class="btn btn-default" type="submit">Login</button> -->
        <%= f.button :submit, "Log in", class:'btn btn-default' %>
    </div>
    <% end %>
    <%= render "devise/shared/links" %>
</form>
<!-- Material form login -->

</div>
</div>
